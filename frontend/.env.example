VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key
# Leave blank when this frontend is served by the same Render service. If the
# frontend is deployed separately, point it to the backend Render URL.
VITE_API_URL=https://beatbond-75ns.onrender.com
# Public Mapbox token (starts with pk.). Create/restrict it in your Mapbox account.
VITE_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_public_token
